<?php $this->headTitle($this->domain['name']) ?>

<h2>
    <?php echo $this->escape($this->domain['name']);?>
    
    <?php
    foreach ($this->domain['servers'] as $server) {
        echo '<span class="minion-server">';

        echo $this->escape($server);

        echo '</span>';
    }
    ?>
</h2>
